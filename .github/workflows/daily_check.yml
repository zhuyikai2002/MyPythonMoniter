name: Blog Monitor

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  check_update:
    runs-on: ubuntu-latest

    # 👇【关键点】这一块必须得有！没有它，Token 就是只读的！
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4

      - name: Run monitor script
        env:
          SERVER_KEY: ${{ secrets.SERVER_KEY }}
        run: python monitor.py

      - name: Commit and push if changed
        run: |
          git config --global user.name "Rik (Action)"
          git config --global user.email "your_email@example.com"
            
          git add last_title.txt
            
          if git diff --staged --quiet; then
            echo "💤 暂无变化"
          else
            git commit -m "Update last_title via PAT"
            # 👇 核武器在这里：使用 MY_PAT 伪装成你本人进行推送
            git push "https://${{ secrets.MY_PAT }}@github.com/${{ github.repository }}.git" HEAD:main
          fi